// this is another way to do it instead of export default Button. 
// can write Button as an object using arrow function
export const Button = () => {
  return <button>Click me</button>
}

// export default Button